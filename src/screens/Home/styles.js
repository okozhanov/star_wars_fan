import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  loaderContainer: {
    position: 'absolute',
    width: '100%',
    height: '100%',
    left: 0,
    right: 0,

    justifyContent: 'center',
    alignItems: 'center',
  },
});
